#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    short T;
    cin >> T;
    for (short t = 1; t <= T; t++) {
        short N;
        cin >> N;
        int A, B;
        cin >> A >> B;
        for (int i = 0; i < N; i++) {
            int u, v;
            cin >> u >> v;
        }
        cout << "Material Management " << t << "\n";
        cout << "Classification ---- End!\n";
    }
    return 0;
}
