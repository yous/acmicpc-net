#include <algorithm>
#include <iostream>
#include <vector>

using namespace std;

char ch;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> ch;
    cout << static_cast<int>(ch) << "\n";
    return 0;
}
